version: '3.8'
services:

  nextjs-app:
    image: nextjs-app
    container_name: nextjs-app
    build:
      context: .
    ports:
      - 4200:4200
    volumes:
      - .:/app
      - /app/node_modules 

volumes:
  node_modules:
  build: